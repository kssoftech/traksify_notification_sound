name: traksify_notification_sound
description: Traksify custom Android notification sound plugin.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.traksify.notification_sound
        pluginClass: TraksifyNotificationSoundPlugin
